    //Coding Train tutorial https://www.youtube.com/watch?v=YcdldZ1E9gU


    

 

   
var inc = 0.1;
var scl = 10;
var cols, rows;

var fr;

function setup() {
    createCanvas(1100, 1700);
    cols = floor(width / scl);
    rows = floor(height / scl);
    fr = createP('');
  }
  
  function draw() {
        background(255);
    var yoff = 0;
    for (var y = 0; y < rows; y++) {
      var xoff = 0;
      for (var x = 0; x < cols; x++) {
        var index = (x + y * width) * 4;
        var angle = noise(xoff, yoff) * TWO_PI;
        var v = p5.Vector.fromAngle(angle);
        xoff += inc;
        stroke(0);
        push();
        translate(x * scl, y * scl);
        rotate(v.heading());
        line(0, 0, scl, 0);

        pop();
      }
      yoff += inc;
    }

    fr.html(floor(frameRate()));
  }
      






























//WAVING TREES
// var inc = 0.1;
// var scl = 10;
// var cols, rows;
// var fr;

// function setup() {
//     createCanvas(1100, 1700);
//     cols = floor(width / scl);
//     rows = floor(height / scl);
//     fr = createP('');
//   }
  
//   function draw() {
//         background(255);
//         // randomSeed(10); //freezes dancing
//     var yoff = 0;
//     for (var y = 0; y < rows; y++) {
//       var xoff = 0;
//       for (var x = 0; x < cols; x++) {
//         var index = (x + y * width) * 4;
//         var angle = noise(xoff, yoff) * TWO_PI;
//         var v = p5.Vector.fromAngle(random(angle));
//         xoff += inc;
//         stroke(0);
//         push();
//         translate(x * scl, y * scl);
//         rotate(v.heading());
//         line(0, 0, scl, 0);

//         pop();
//       }
//       yoff += inc;
//     }

//     fr.html(floor(frameRate()));
//   }


//SWERVING DASHES
// var inc = 0.1;
//     var scl = 10;
//     var cols, rows;
//     var fr;
    
//     function setup() {
//         createCanvas(1100, 1700);
//         cols = floor(width / scl);
//         rows = floor(height / scl);
//         fr = createP('');
//       }
      
//       function draw() {
//             background(255);
//             //  randomSeed(10); //freezes dancing
//         var yoff = 0;
//         for (var y = 0; y < rows; y++) {
//           var xoff = 0;
//           for (var x = 0; x < cols; x++) {
//             var index = (x + y * width) * 4;
//             var r = noise(xoff, yoff) * 255;
//             var v = p5.Vector.fromAngle(random(TWO_PI));
//             xoff += inc;
//             stroke(0);
//             push();
//             translate(x * scl, y * scl);
//             rotate(v.heading());
//             line(0, 0, scl, 0);
    
//             pop();
//           }
//           yoff += inc;
//         }
    
//         fr.html(floor(frameRate()));
//       }


//SPIDERY CRYSTALIZATION!!
// var inc = 0.1;
// var scl = 10;
// var cols, rows;

// function setup() {
//     createCanvas(1100, 1700);
//     cols = floor(width / scl);
//     rows = floor(height / scl);
//   }
  
//   function draw() {
//       //   background(255); //stops blackscreening
//             // randomSeed(10); //freezes it.. replace # with randomizer to get variety on click

//     var yoff = 0;
//     for (var y = 0; y < rows; y++) {
//       var xoff = 0;
//       for (var x = 0; x < cols; x++) {
//         var index = (x + y * width) * 4;
//         var r = noise(xoff, yoff) * 255;
//         var v = p5.Vector.fromAngle(random(TWO_PI));
//         xoff += inc;
//         stroke(0);
//         push();
//         translate(x * scl, y * scl);
//         rotate(v.heading());
//         line(0, 0, scl, 0);

//         pop();
//       }
//       yoff += inc;
//     }
//     // noLoop();
//   }


    //STRIPES
//     var inc = 0.1;
// var scl = 10;
// var cols, rows;

// function setup() {
//     createCanvas(1100, 1700);
//     cols = floor(width / scl);
//     rows = floor(height / scl);
//   }
  
//   function draw() {
//     var yoff = 0;
//     for (var y = 0; y < rows; y++) {
//       var xoff = 0;
//       for (var x = 0; x < cols; x++) {
//         var index = (x + y * width) * 4;
//         var r = noise(xoff, yoff) * 255;
//         var v = p5.Vector.fromAngle(0); // 0 = horizontal lines, PI/2 = vertical lines, PI/6 = dashed lines
//         xoff += inc;
//         stroke(0);
//         push();
//         translate(x * scl, y * scl);
//         rotate(v.heading());
//         line(0, 0, scl, 0);

//         pop();
//       }
//       yoff += inc;
//     }
//   }


//SMOKEY GRID
// var inc = 0.1;
// var scl = 10;
// var cols, rows;

// function setup() {
//     createCanvas(1100, 1700);
//     cols = floor(width / scl);
//     rows = floor(height / scl);
//   }
  
//   function draw() {
//     var yoff = 0;
//     for (var y = 0; y < rows; y++) {
//       var xoff = 0;
//       for (var x = 0; x < cols; x++) {
//         var index = (x + y * width) * 4;
//         var r = noise(xoff, yoff) * 255;
//         xoff += inc;
//         fill(r);
//         rect(x * scl, y * scl, scl, scl)
//       }
//       yoff += inc;
//     }
//   // noLoop();
//   }





    //DANCING GRID!!!
//   var inc = 0.01;
//       var scl = 20;
//       var cols, rows;
      
//       function setup() {
//           createCanvas(1100, 1700);
//           cols = floor(width / scl);
//           rows = floor(height / scl);
//         }
        
//         function draw() {
//           var yoff = 0;
//           for (var y = 0; y < rows; y++) {
//             var xoff = 0;
//             for (var x = 0; x < cols; x++) {
//               var index = (x + y * width) * 4;
//               var r = noise(xoff, yoff) * 255;
//               xoff += inc;
//               fill(random(255));
//               rect(x * scl, y * scl, scl, scl)
//             }
//             yoff += inc;
//           }
//         // noLoop();
//         }



//SMOKE!!! the most smoke i could smoke

// var inc = 0.01;
// var start = 0;

//  function setup() {
//         createCanvas(1100, 1700);
//       }

//       function draw() {
//         var yoff = 0;
//         loadPixels();
//         for (var y = 0; y < height; y++) {
//           var xoff = 0;
//           for (var x = 0; x < width; x++) {
//             var index = (x + y * width) * 4;
//             var r = noise(xoff, yoff) * 255; //brightness
//             pixels[index + 0] = r; //red
//             pixels[index + 1] = r; //green
//             pixels[index + 2] = r; //blue
//             pixels[index + 3] = 255; //alpha
//             xoff += inc;
//           }
//           yoff += inc;
//           noiseDetail(150); //pixelation happens here
//         }
//       updatePixels();
//       // noLoop();
//       }












      //SMOOTH BLURRED CANVAS!!!!!!
// var inc = 0.001; //mess with this for smoothness
// var start = 0;


// function setup() {
//        createCanvas(1100, 1700);
//      }

//      function draw() {
//        var yoff = 0;
//        loadPixels();
//        for (var y = 0; y < height; y++) {
//          var xoff = 0;
//          for (var x = 0; x < width; x++) {
//            var index = (x + y * width) * 4;
//            var r = noise(xoff, yoff) * 255; //brightness
//            pixels[index + 0] = r; //red
//            pixels[index + 1] = r; //green
//            pixels[index + 2] = r; //blue
//            pixels[index + 3] = 255; //alpha
//            xoff += inc;
//          }
//          yoff += inc;
//          noiseDetail(1); //pixelation happens here
//        }
//      updatePixels();
//      // noLoop();
//      }





      //SMOKE
    //   function draw() {
    //     var yoff = 0;
    //     loadPixels();
    //     for (var y = 0; y < height; y++) {
    //       var xoff = 0;
    //       for (var x = 0; x < width; x++) {
    //         var index = (x + y * width) * 4;
    //         var r = noise(xoff, yoff) * 255; //brightness
    //         pixels[index + 0] = r; //red
    //         pixels[index + 1] = r; //green
    //         pixels[index + 2] = r; //blue
    //         pixels[index + 3] = 255; //alpha
    //         xoff += inc;
    //       }
    //       yoff += inc; //smaller inc value blurs more
    //     }
    //   updatePixels();
    //   }







//HORIZONTAL BLURRED LINES
    //   function draw() {
    //     var yoff = 0;
    //     loadPixels();
    //     for (var x = 0; x < width; x++) {
    //     var xoff = 0;
    //           for (var y = 0; y < height; y++) {
    //         var index = (x + y * width) * 4;
    //         var r = noise(xoff, yoff) * 255; //brightness
    //         pixels[index + 0] = r; //red
    //         pixels[index + 1] = r; //green
    //         pixels[index + 2] = r; //blue
    //         pixels[index + 3] = 255; //alpha
    //         xoff += 0.01;
    //       }
    //     }
    //   updatePixels();
    //   }





//VERTICAL BLURRED LINES
      // function draw() {
      //   var yoff = 0;
      //   loadPixels();
      //   for (var y = 0; y < height; y++) {
      //     var xoff = 0;
      //     for (var x = 0; x < width; x++) {
      //       var index = (x + y * width) * 4;
      //       var r = noise(xoff, yoff) * 255; //brightness
      //       pixels[index + 0] = r; //red
      //       pixels[index + 1] = r; //green
      //       pixels[index + 2] = r; //blue
      //       pixels[index + 3] = 255; //alpha
      //       xoff += 0.01;
      //     }
      //   }
      // updatePixels();
      // }







//HORIZONTAL LONG DASHES
// function draw() {
//     loadPixels();
//     var yoff = 0;

//     for (var y = 0; y < height; y++) {
//         for (var x = 0; x < width; x++) {
//         var index = (x + y * height) * 4;
//         var r = noise(yoff) * 255; //brightness
//         pixels[index + 0] = r; //red
//         pixels[index + 1] = r; //green
//         pixels[index + 2] = r; //blue
//         pixels[index + 3] = 255; //alpha

//   yoff += 0.01; //CREATES STREAKS //move y to outer loop to get lines across
//       }
//     }
//   updatePixels();
//   }



      //HORIZONTAL STROKES
    //   function draw() {
    //     loadPixels();
    //     var xoff = 0;
    
    //     for (var y = 0; y < height; y++) {
    //           for (var x = 0; x < width; x++) {
    //         var index = (x + y * width) * 4;
    //         var r = noise(xoff) * 255; //brightness
    //         pixels[index + 0] = r; //red
    //         pixels[index + 1] = r; //green
    //         pixels[index + 2] = r; //blue
    //         pixels[index + 3] = 255; //alpha

    //   xoff += 0.01; //CREATES STREAKS //move y to outer loop to get lines across
    //       }
    //     }
    //   updatePixels();
    //   }



//VERTICAL STROKES
// function draw() {
//         loadPixels();
//         var xoff = 0;
    
//         for (var x = 0; x < width; x++) {
//           for (var y = 0; y < height; y++) {
//             var index = (x + y * width) * 4;
//             var r = noise(xoff) * 255; //brightness
//             pixels[index + 0] = r; //red
//             pixels[index + 1] = r; //green
//             pixels[index + 2] = r; //blue
//             pixels[index + 3] = 255; //alpha

//       xoff += 0.01; //CREATES STREAKS //move y to outer loop to get lines across
//           }
//         }
//       updatePixels();
//       }








//  function setup() {
//         createCanvas(1100, 1700);
//       }
      
//       function draw() {
//         loadPixels();
    
//         for (var x = 0; x < width; x++) {
//           for (var y = 0; y < height; y++) {
//             var index = (x + y * width) * 4;
//             var r = random(255); //brightness
//             pixels[index + 0] = r; //red
//             pixels[index + 1] = r; //green
//             pixels[index + 2] = r; //blue
//             pixels[index + 3] = 255; //alpha
//           }
//         }
//       updatePixels();
//       }





// function setup() {
//     createCanvas(1100, 1700);
//   }
  
//   function draw() {
//     background(51);

//     //THIS CREATES A LINE GRAPH
//     // START MAKES GRAPH MOVE
//     // stroke(255);
//     // noFill();
//     // beginShape();
//     // // var xoff = 0;
//     // var xoff = start;
//     // for (var x = 0; x < width; x++) {
//     //   stroke(255);
//     //   var y = random(height);
//     //   var y = map(noise(xoff), 0, 1, 0, height);
//     //   var y = height/2 + map(sin(xoff), 0, 1, 0, height) * height/2; //CREATES SINE WAVE
//     //   var y = height/2 + map(sin(xoff), 0, 1, 0, height) * height/2; //CREATES STRAIGHT LINES
//     //   var y = height/2 + sin(xoff) * height/2; //CREATES SMALLER SINE WAVES
//     //   var y = noise(xoff) * 300 + height/2 + sin(xoff) * height/2; //CREATES WOBBLE TO SINE WAVES
//     //   var n = map(noise(xoff), 0, 1, -50, 50); //ADDS NOISE TO SINE WAVE
//     //   var s = map(sin(xoff), -1, 1, 0, height);
      
      
//     //   var n = map(noise(xoff), 0, 1, 0, height); //ADDS SINE TO NOISE WAVE
//     //   var s = map(sin(xoff), -1, 1, -50, 50);
//     //   var y = s + n;
//     //   vertex(x, y);
      
      
      
//     //   //xoff += 0.01;
//     //   xoff += inc;
//     // }
//     // endShape();
//     // start += inc;


//     //THIS CREATES RANDOM LINES FOR RANDOM VALUES ACROSS X AXIS
//     // stroke(255);
//     // noFill();
//     // beginShape();
//     // for (var x = 0; x < width; x++) {
//     //   stroke(255);
//     //   vertex(x, random(height));
//     // }
//     // endShape();

//     //THIS CREATES RANDOM LINES FOR RANDOM VALUES ACROSS Y AXIS
//     // stroke(255);
//     // noFill();
//     // beginShape();
//     // for (var y = 0; y < height; y++) {
//     //   stroke(255);
//     //   vertex(random(width), y);
//     // }
//     // endShape();

    
//     //THIS CREATES GRAINY NOISE
//     // for (var x = 0; x < innerWidth; x++) {
//     //   stroke(255);
//     //   point(x, random(height));
//     // }
//     // noLoop(); //FREEZES NOISE


//     //THIS CREATES RANDOM X VALUES EVERY SECOND
//     // var x = random(width);


//     //THIS CREATES A ROVING CIRCLE
//     // var x = map(noise(xoff), 0, 1, 0, width);
//     // var y = map(noise(yoff), 0, 1, 0, height);
//     // xoff += 0.01;
//     // yoff += 0.01;
//     // ellipse (x, y, 25, 25);
//   }


//   //noise();
//   //noise range is 0-1
//   //perlin curves around .5


